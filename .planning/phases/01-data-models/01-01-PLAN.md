---
phase: 01-data-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REQ-39
user_setup: []

must_haves:
  truths:
    - Category model class exists with all required properties
    - CategoryType enum exists for categorizing events
    - Validation attributes are applied to required fields
    - Model compiles without errors
  artifacts:
    - path: Models/Category.cs
      provides: Main category entity
      contains: CategoryId, CategoryName, CategoryDescription, CategoryType, IsActive, CreatedDate, UpdatedDate
    - path: Models/CategoryType.cs
      provides: Category type enumeration
      contains: enum CategoryType
  key_links:
    - from: Category.cs
      to: CategoryType.cs
      via: using directive
      pattern: CategoryType
---

<objective>
Create the Category model and CategoryType enum for the Event Categories module.

Purpose: Establish the data model foundation that all subsequent phases depend on.
Output: Models/Category.cs, Models/CategoryType.cs with validation
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Project Context
- RootNamespace: Campus
- Framework: .NET MAUI (net10.0-windows10.0.19041.0)
- Pattern: MVVM with CommunityToolkit.Mvvm
- Existing Views in: Views/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryType enum</name>
  <files>Models/CategoryType.cs</files>
  <action>
Create CategoryType.cs in the Models folder with an enum for event categories.

Required categories based on typical campus events:
- Academic (conferences, seminars, workshops)
- Sports (competitions, tournaments, matches)
- Cultural (performances, exhibitions, festivals)
- Social (meetups, networking, parties)
- Volunteer (community service, charity)
- Other (miscellaneous events)

Use:
```csharp
namespace Campus.Models;

public enum CategoryType
{
    Academic,
    Sports,
    Cultural,
    Social,
    Volunteer,
    Other
}
```
  </action>
  <verify>
    <automated>dotnet build Campus.csproj 2>&1 | Select-String -Pattern "error|warning" | Select-Object -First 5</automated>
  </verify>
  <done>CategoryType enum exists with all 6 category types, compiles successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create Category model class</name>
  <files>Models/Category.cs</files>
  <action>
Create Category.cs in the Models folder with all required properties per #39:

Properties:
- CategoryId (int, primary key) - use [Key] attribute
- CategoryName (string, required) - use [Required] and [MaxLength(100)]
- CategoryDescription (string, optional) - use [MaxLength(500)]
- CategoryType (CategoryType enum)
- IsActive (bool, default true)
- CreatedDate (DateTime) - set to DateTime.Now in constructor
- UpdatedDate (DateTime) - set to DateTime.Now in constructor

Use System.ComponentModel.DataAnnotations for validation attributes.
Use System.ComponentModel.DataAnnotations.Schema for [Key].

Follow conventions from CONVENTIONS.md:
- File-scoped namespace: namespace Campus.Models;
- PascalCase for class and properties
- No prefix for fields
  </action>
  <verify>
    <automated>dotnet build Campus.csproj 2>&1 | Select-String -Pattern "error|warning" | Select-Object -First 5</automated>
  </verify>
  <done>Category model has all 7 properties with correct types and validation attributes</done>
</task>

</tasks>

<verification>
[ ] Category.cs compiles without errors
[ ] CategoryType.cs compiles without errors
[ ] Both files are in the Models folder
[ ] Validation attributes are applied to CategoryName (Required, MaxLength) and CategoryDescription (MaxLength)
</verification>

<success_criteria>
Phase 1 complete when:
- [ ] Category model exists with all required properties (#39)
- [ ] CategoryType enum is defined
- [ ] Validation attributes are applied
- [ ] Code compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-models/01-01-SUMMARY.md`
</output>
