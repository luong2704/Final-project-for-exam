---
phase: 02-service-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - Data/MockCategoryDataProvider.cs
  - Data/categories.json
  - Services/CategoryService.cs
autonomous: true
requirements:
  - REQ-40
user_setup: []

must_haves:
  truths:
    - categories.json exists with sample category data
    - MockCategoryDataProvider loads data from JSON correctly
    - CategoryService uses MockCategoryDataProvider to return data
    - GetAllCategoriesAsync returns all active categories
  artifacts:
    - path: Data/categories.json
      provides: Initial category data
      contains: JSON array of categories
    - path: Data/MockCategoryDataProvider.cs
      provides: Data access layer for mock data
      contains: class MockCategoryDataProvider
  key_links:
    - from: CategoryService.cs
      to: MockCategoryDataProvider.cs
      via: dependency injection or instantiation
      pattern: MockCategoryDataProvider
---

<objective>
Implement the mock data provider and finalize the CategoryService implementation.

Purpose: Provide a working data source for categories using JSON and a mock provider.
Output: Data/categories.json, Data/MockCategoryDataProvider.cs, Updated Services/CategoryService.cs
</objective>

<context>
@.planning/REQUIREMENTS.md
@Services/ICategoryService.cs
@Services/CategoryService.cs
@Models/Category.cs
@Models/CategoryType.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create categories.json</name>
  <files>Data/categories.json</files>
  <action>
Create categories.json in the Data folder with sample data for all 6 CategoryTypes.

Required fields: CategoryId, CategoryName, CategoryDescription, CategoryType, IsActive, CreatedDate, UpdatedDate.

Ensure CategoryType values match the enum strings: Academic, Sports, Cultural, Social, Volunteer, Other.
  </action>
  <verify>
    File exists and contains valid JSON array with 6+ items
  </verify>
  <done>categories.json contains sample data for all category types</done>
</task>

<task type="auto">
  <name>Task 2: Implement MockCategoryDataProvider</name>
  <files>Data/MockCategoryDataProvider.cs</files>
  <action>
Create MockCategoryDataProvider.cs in the Data folder.
It should read categories.json and deserialize it into a list of Category objects.

Use System.Text.Json for deserialization.
  </action>
  <verify>
    <automated>dotnet build Campus.csproj 2>&1 | Select-String -Pattern "error|warning" | Select-Object -First 5</automated>
  </verify>
  <done>MockCategoryDataProvider correctly reads and parses JSON data</done>
</task>

<task type="auto">
  <name>Task 3: Finalize CategoryService</name>
  <files>Services/CategoryService.cs</files>
  <action>
Update CategoryService to use MockCategoryDataProvider to return data in its methods.

- GetAllCategoriesAsync(): returns all where IsActive is true
- GetCategoryByIdAsync(int id): returns first match or null
- GetCategoriesByTypeAsync(CategoryType type): returns matches where IsActive is true
  </action>
  <verify>
    <automated>dotnet build Campus.csproj 2>&1 | Select-String -Pattern "error|warning" | Select-Object -First 5</automated>
  </verify>
  <done>CategoryService methods are fully implemented and functional</done>
</task>

</tasks>

<verification>
[ ] Data/categories.json contains valid data
[ ] Data/MockCategoryDataProvider.cs handles JSON parsing
[ ] CategoryService.cs returns data from provider
[ ] Project builds without errors
</verification>

<success_criteria>
- [ ] Mock data source integrated
- [ ] CategoryService fully implemented
- [ ] Data operations functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-layer/02-02-SUMMARY.md`
</output>
