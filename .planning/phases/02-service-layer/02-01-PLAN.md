---
phase: 02-service-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Services/ICategoryService.cs
  - Services/CategoryService.cs
autonomous: true
requirements:
  - REQ-40
user_setup: []

must_haves:
  truths:
    - ICategoryService interface defines all required methods
    - CategoryService implements ICategoryService
    - CategoryService compiles successfully
  artifacts:
    - path: Services/ICategoryService.cs
      provides: Service contract for categories
      contains: interface ICategoryService
    - path: Services/CategoryService.cs
      provides: Service implementation for categories
      contains: class CategoryService
  key_links:
    - from: CategoryService.cs
      to: ICategoryService.cs
      via: inheritance
      pattern: ": ICategoryService"
---

<objective>
Define the ICategoryService interface and its initial implementation class.

Purpose: Create the service layer contract and base implementation that the UI and business logic will consume.
Output: Services/ICategoryService.cs, Services/CategoryService.cs
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@Models/Category.cs
@Models/CategoryType.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ICategoryService interface</name>
  <files>Services/ICategoryService.cs</files>
  <action>
Create ICategoryService.cs in the Services folder.

Required methods per #40:
- Task&lt;IEnumerable&lt;Category&gt;&gt; GetAllCategoriesAsync();
- Task&lt;Category&gt; GetCategoryByIdAsync(int id);
- Task&lt;IEnumerable&lt;Category&gt;&gt; GetCategoriesByTypeAsync(CategoryType type);

Use:
```csharp
using Campus.Models;

namespace Campus.Services;

public interface ICategoryService
{
    Task&lt;IEnumerable&lt;Category&gt;&gt; GetAllCategoriesAsync();
    Task&lt;Category&gt; GetCategoryByIdAsync(int id);
    Task&lt;IEnumerable&lt;Category&gt;&gt; GetCategoriesByTypeAsync(CategoryType type);
}
```
  </action>
  <verify>
    <automated>dotnet build Campus.csproj 2>&1 | Select-String -Pattern "error|warning" | Select-Object -First 5</automated>
  </verify>
  <done>ICategoryService interface exists with all 3 async methods</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryService stub</name>
  <files>Services/CategoryService.cs</files>
  <action>
Create CategoryService.cs in the Services folder implementing ICategoryService.
For now, implement methods as throwing NotImplementedException or returning empty results. Implementation with mock data comes in the next plan.

Use:
```csharp
using Campus.Models;

namespace Campus.Services;

public class CategoryService : ICategoryService
{
    public Task&lt;IEnumerable&lt;Category&gt;&gt; GetAllCategoriesAsync()
    {
        throw new NotImplementedException();
    }

    public Task&lt;Category&gt; GetCategoryByIdAsync(int id)
    {
        throw new NotImplementedException();
    }

    public Task&lt;IEnumerable&lt;Category&gt;&gt; GetCategoriesByTypeAsync(CategoryType type)
    {
        throw new NotImplementedException();
    }
}
```
  </action>
  <verify>
    <automated>dotnet build Campus.csproj 2>&1 | Select-String -Pattern "error|warning" | Select-Object -First 5</automated>
  </verify>
  <done>CategoryService class exists and implements ICategoryService</done>
</task>

</tasks>

<verification>
[ ] ICategoryService.cs exists in Services/
[ ] CategoryService.cs exists in Services/
[ ] Project builds without errors
</verification>

<success_criteria>
- [ ] Service contract defined
- [ ] Initial service implementation exists
- [ ] Project compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-layer/02-01-SUMMARY.md`
</output>
